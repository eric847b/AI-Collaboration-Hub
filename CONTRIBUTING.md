# Contributing

Thanks for considering contributing. For minor changes, open a PR against `main`. For larger changes, open an issue first to discuss.

- Please follow the formatting rules in `.editorconfig`.
- Run `tools/bootstrap.ps1` to install dependencies locally.
- All PRs should include tests where appropriate.
