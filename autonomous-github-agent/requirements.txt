# Runtime dependency for the mirrored fleet-maintenance engine.
# fleet_maintenance.py imports `requests` (mirrored from standalone PR 322).
# NOTE: aligned with pyproject.toml; do NOT pin versions that don't exist on PyPI.
requests>=2.28.0
pytest>=8.0.0
PyGithub>=2.0.0
