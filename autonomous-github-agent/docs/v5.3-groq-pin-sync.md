# v5.3 — Groq pin sync

**Date:** 2026-08-05

## Change

- `KNOWN_PY_PINS["groq"]` updated from `groq>=0.4.0` → `groq>=1.6.0`
- Aligns with `singularity-operator` v0.6 (restored `groq_wrapper.py` + SDK 1.6 client)
- Auto-generated `requirements.txt` for any Python tree that imports `groq` will now pin the current major

## Why

After the singularity-operator groq major upgrade, the solver map still advertised the old lower bound. That would have re-introduced an under-pin on the next missing-requirements fix cycle.

## Verification

- `singularity-operator/requirements.txt` and `pyproject.toml` already declare `groq>=1.6.0`
- Note file `problem_solvers_groq_note.md` already documented the target pin
