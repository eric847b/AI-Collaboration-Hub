# Changelog

## [1.0.0] — 2026-09-29

### Added
- Initial scaffold: `generateScript`, `previewTemplate`, and `openOptions` commands
- Sidebar dashboard webview view (`aiAssistantDashboard`)
- Settings schema (`aiAssistant.apiKey`, `aiAssistant.defaultProvider`, `aiAssistant.model`)
- Webview template preview with HTML escaping
- Packaging support (`.vscodeignore`, `vsce` instructions) and debug/task profiles