# .npmrc
# Package-lock resets are more than just operational noise
# for this monorepo setup, so allow them when deliberate.
# package-lock=false

# Reduce noise and CI churn from funding/audit messages.
fund=false
audit=false

# Keep installs deterministic across machines.
save-exact=true
legacy-peer-deps=false