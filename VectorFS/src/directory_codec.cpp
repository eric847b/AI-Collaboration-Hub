#include "directory_codec.h"

std::vector<unsigned char> encode_directory(const std::vector<DirEntry>&) {
    return {};
}

std::vector<DirEntry> decode_directory(const std::vector<unsigned char>&) {
    return {};
}
